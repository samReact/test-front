import React from "react";
import { StyledResetButton } from "../styled/style";

const ResetButton = () => {
  return <StyledResetButton type="button">Réinitialiser</StyledResetButton>;
};

export default ResetButton;
